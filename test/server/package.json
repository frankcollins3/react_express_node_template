{
  "name": "test",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "nodemon ./bin/www",
    "client": "cd client && npm start"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "~4.16.1",
    "nodemon": "^2.0.22"
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
